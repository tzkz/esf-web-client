import React from 'react';
import SectionContent from './common/SectionContent';

const Search = () => (
  <div>
    <SectionContent>
      search page
    </SectionContent>
  </div>
);

export default Search;
