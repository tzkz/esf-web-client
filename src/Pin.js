import React from 'react';
import { css } from 'emotion';

const container = {
  backgroundColor: 'palegreen',
}

const Pin = () => (
  <div className={css(container)}>
    Pin
  </div>
);

export default Pin;
